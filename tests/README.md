# Tests

This project uses NUnit. The test project lives at `tests/Encode.Tests`.

## Run tests locally

From the repo root:

```
dotnet test tests/Encode.Tests/Encode.Tests.csproj
```

To restore packages explicitly (if needed):

```
dotnet restore tests/Encode.Tests/Encode.Tests.csproj
```
